/*Copyright © AsukiAoyama 2023*/

let color0 = (parseInt(background0, 16) * l).toString(16);
let color1 = (parseInt(background1, 16) * l).toString(16);
let color2 = (parseInt(background2, 16) * l).toString(16);
let color3 = (parseInt(background3, 16) * l).toString(16);
let color4 = (parseInt(background4, 16) * l).toString(16);
        /* var n = 1500;
    
    var a = [],y = [],x = [],s = [],strs = [],
    d = document,
    cols = ['#9db4ff', '#a2b9ff', '#a7bcff',
    '#aabfff', '#afc3ff', '#baccff', '#c0d1ff',
    '#cad8ff', '#e4e8ff', '#edeeff', '#fbf8ff',
    '#fff9f9', '#fff5ec', '#fff4e8', '#fff1df',
    '#ffebd1', '#ffd7ae', '#ffc690', '#ffbe7f',
    '#ffbb7b', '#ffbb7b', '#fff', '#fff'];
    
    var container = d.createElement('div');
    container.setAttribute('style','display:block;'
    +'positfor (var i = 0; i < n; i++){
        var col = cols[Math.random() * cols.length | 0];
        strs[i] = d.createElement('div');
        strs[i].setAttribute('style', 'display:block;'
        +'position:absolute;'
        +'border-radius:50%;'
        +'background-color:#fff;'
        +'box-shadow: 0 0 12px 1.2px '+col+';');
    container.appendChild(strs[i]);
    d.body.appendChild(container);
    
    for (var i = 0; i < n; i++){
        var col = cols[Math.random() * cols.length | 0];
        strs[i] = d.createElement('div');
        strs[i].setAttribute('style', 'display:block;'
        +'position:absolute;'
        +'border-radius:50%;'
        +'background-color:#fff;'
        +'box-shadow: 0 0 12px 1.2px '+col+';');
    container.appendChild(strs[i]);
    }
    for (var i = 0; i < n; i++){
        var col = cols[Math.random() * cols.length | 0];
        strs[i] = d.createElement('div');
        strs[i].setAttribute('style', 'display:block;'
        +'position:absolute;'
        +'border-radius:50%;'
        +'background-color:#fff;'
        +'box-shadow: 0 0 12px 1.2px '+col+';');
    container.appendChild(strs[i]);

    function gen() {
        for (var i = 0; i < n; i++) {
            var r = Math.random() * 100 | 0;
            var cen = (r == 0)? 2.3 : 0.7;
            a[i] = Math.random() * Math.PI * 2;
            y[i] = 50 + Math.random() * 50 / cen * Math.sin(a[i]);
            x[i] = 50 + Math.random() * 50 / cen * Math.cos(a[i]);
            s[i] = 0.4 + Math.random() * 2;
            strs[i].style.transform = 'translate3d('+x[i]+'vw, '+y[i]+'vh, 0)';
            strs[i].style.width = (strs[i].style.height = (s[i]) + 'px');
            strs[i].style.opacity = s[i] / 3;
        }
 
         };*/
let color5 = (parseInt(background5, 16) * l).toString(16);
let color6 = (parseInt(background6, 16) * l).toString(16);
let color7 = (parseInt(background7, 16) * l).toString(16);
let color8 = (parseInt(background8, 16) * l).toString(16);
let color9 = (parseInt(background9, 16) * l).toString(16);
let color10 = (parseInt(background10, 16) * l).toString(16);
        /* var n = 1500;
    
    var a = [],y = [],x = [],s = [],strs = [],
    d = document,
    cols = ['#9db4ff', '#a2b9ff', '#a7bcff',
    '#aabfff', '#afc3ff', '#baccff', '#c0d1ff',
    '#cad8ff', '#e4e8ff', '#edeeff', '#fbf8ff',
    '#fff9f9', '#fff5ec', '#fff4e8', '#fff1df',
    '#ffebd1', '#ffd7ae', '#ffc690', '#ffbe7f',
    '#ffbb7b', '#ffbb7b', '#fff', '#fff'];
    
    var container = d.createElement('div');
    container.setAttribute('style','display:block;'
    +'position:fixed;'
    +'height:100%;width:100%;'
    +'margin:auto;padding:0px;'
    +'top:0;left:0;left:0;right:0;'
    +'background-color:transparent;'
    +'overflow:hidden;');
    d.body.appendChild(container);
    
    for (var i = 0; i < n; i++){
        var col = cols[Math.random() * cols.length | 0];
        strs[i] = d.createElement('div');
        strs[i].setAttribute('style', 'display:block;'
        +'position:absolute;'
        +'border-radius:50%;'
        +'background-color:#fff;'
        +'box-shadow: 0 0 12px 1.2px '+col+';');
    container.appendChild(strs[i]);
    }

    function gen() {
        for (var i = 0; i < n; i++) {
            var r = for (var i = 0; i < n; i++){
        var col = cols[Math.random() * cols.length | 0];
        strs[i] = d.createElement('div');
        strs[i].setAttribute('style', 'display:block;'
        +'position:absolute;'
        +'border-radius:50%;'
        +'background-color:#fff;'
        +'box-shadow: 0 0 12px 1.2px '+col+';');
    container.appendChild(strs[i]);* 2;
            strs[i].style.transform = 'translate3d('+x[i]+'vw, '+y[i]+'vh, 0)';
            strs[i].style.width = (strs[i].style.height = (s[i]) + 'px');
            strs[i].style.opacity = s[i] / 3;
        }
 
         };*/

let function0 ="01010100 01100001 01110101 01110010 01110101 01110011";
let function1 ="01010011 01100101 01110000 01110000 01101100";
let function2 ="01010100 01101111 01110100 01101000";
let function3 ="01001011 01100001 01100010 01101111 01110011 01100011 01101000 01101011 01100101";
let function4 ="01001111 01110100 01110100 01101111";
        /* var n = 1500;
    
    var a = [],y = [],x = [],s = [],strs = [],
    d = document,
    cols = ['#9db4ff', '#a2b9ff', '#a7bcff',
    '#aabfff', '#afc3ff', '#baccff', '#c0d1ff',
    '#cad8ff', '#e4e8ff', '#edeeff', '#fbf8ff',
    '#fff9f9', '#fff5ec', '#fff4e8', '#fff1df',
    '#ffebd1', '#ffd7ae', '#ffc690', '#ffbe7f',
    '#ffbb7b', '#ffbb7b', '#fff', '#fff'];
    
    var container = d.createElement('div');
    container.setAttribute('style','display:block;'
    +'position:fixed;'
    +'height:100%;width:100%;'
    +'margin:auto;padding:0px;'
    +'top:0;left:0;left:0;right:0;'
    +'background-color:transparent;'
    +'overflow:hidden;');
    d.body.appendChild(container);
    
    for (var for (var i = 0; i < n; i++){
        var col = cols[Math.random() * cols.length | 0];
        strs[i] = d.createElement('div');
        strs[i].setAttribute('style', 'display:block;'
        +'position:absolute;'
        +'border-radius:50%;'
        +'background-color:#fff;'
        +'box-shadow: 0 0 12px 1.2px '+col+';');
    container.appendChild(strs[i]);
        +'background-color:#fff;'
        +'box-shadow: 0 0 12px 1.2px '+col+';');
    container.appendChild(strs[i]);
    }

    function gen() {
        for (var i = 0; i < n; i++) {
            var r = Math.random() * 100 | 0;
            var cen = (for (var i = 0; i < n; i++){
        var col = cols[Math.random() * cols.length | 0];
        strs[i] = d.createElement('div');
        strs[i].setAttribute('style', 'display:block;'
        +'position:absolute;'
        +'border-radius:50%;'
        +'background-color:#fff;'
        +'box-shadow: 0 0 12px 1.2px '+col+';');
    container.appendChild(strs[i]);() * 50 / cen * Math.cos(a[i]);
            s[i] = 0.4 + Math.random() * 2;
            strs[i].style.transform = 'translate3d('+x[i]+'vw, '+y[i]+'vh, 0)';
            strs[i].style.width = (strs[i].style.height = (s[i]) + 'px');
            strs[i].style.opacity = s[i] / 3;
        }
 
         };*/
let function5 ="01001111 01110100 01110100 01101111 01001111 01110100 01110100";
let function6 ="01001111 01110100 01110100 01101111 01001111 01110100 01110100";
let function7 ="01001111 01010100 01110100 01101111 01001111 01110100 01110100 01101111";
let function8 ="01001111 01110100 01100100 01101111 01001111 01110100 01110100 01101111";
let function9 ="01001111 01110100 01110100 01101111 01001111 01110100 01110100 ";
let function10 ="01001111 01110100 01110100 01101111 01001111 01110100 01110100 01101111";
        /* var n = 1500;
    
    var a = [],y = [],x = [],s = [],strs = [],
    d = document,
    cols = ['#9db4ff', '#a2b9ff', '#a7bcff',
    '#aabfff', '#afc3ff', '#baccff', '#c0d1ff',
    '#cad8ff', '#e4e8ff', '#edeeff', '#fbf8ff',
    '#fff9f9', '#fff5ec', '#fff4e8', '#fff1df',
    '#ffebd1', '#ffd7ae', '#ffc690', '#ffbe7f',
    '#ffbb7b', '#ffbb7b', '#fff', '#fff'];
    
    var container = d.createElement('div');
    container.setAttribute('style','display:block;'
    +'position:fixed;'
    +'height:100%;width:100%;'
    +'margin:auto;padding:0px;'
    +'top:0;left:0;left:0;right:0;'
    +'background-color:transparent;'
    +'overflow:hidden;');
    d.body.appendChild(container);
    
    for (var i = 0; i < n; i++){
        var col = cols[Math.random() * cols.length | 0];
        strs[i] = d.createElement('div');
        strs[i].setAttribute('style', 'display:block;'
        +'position:absolute;'
        +'border-radius:50%;'
        +'background-color:#fff;'
        +'box-shadow: 0 0 12px 1.2px '+col+';');
    container.appendChild(strs[i]);
    }

    function gen() {
        for (var i = 0; i < n; i++) {
            var r = Math.random() * 100 | 0;
            var cen = (r == 0)? 2.3 : 0.7;
            a[i] = Math.random() * Math.PI * 2;
            y[i] = 50 + Math.random() * 50 / cen * Math.sin(a[i]);
            x[i] = 50 + Math.random() * 50 / cen * Math.cos(a[i]);
            s[i] = 0.4 + Math.random() * 2;
            strs[i].style.transform = 'translate3d('+x[i]+'vw, '+y[i]+'vh, 0)';
            strs[i].style.width = (strs[i].style.height = (s[i]) + 'px');
            strs[i].style.opacity = s[i] / 3;
        }
 
         };*/
let inner0 = (parseInt(color0, 16) * y).toString(16);
let inner1 = (parseInt(color1, 16) * y).toString(16);
let inner2 = (parseInt(color2, 16) * y).toString(16);
let inner3 = (parseInt(color3, 16) * y).toString(16);
let inner4 = (parseInt(color4, 16) * y).toString(16);
        /* var n = 1500;
    
    var a = [],y = [],x = [],s = [],strs = [],
    d = document,
    cols = ['#9db4ff', '#a2b9ff', '#a7bcff',
    '#aabfff', '#afc3ff', '#baccff', '#c0d1ff',
    '#cad8ff', '#e4e8ff', '#edeeff', '#fbf8ff',
    '#fff9f9', '#fff5ec', '#fff4e8', '#fff1df',
    '#ffebd1', '#ffd7ae', '#ffc690', '#ffbe7f',
    '#ffbb7b', '#ffbb7b', '#fff', '#fff'];
    
    var container = d.createElement('div');
    container.setAttribute('style','display:block;'
    +'position:fixed;'
    +'height:100%;width:100%;'
    +'margin:auto;padding:0px;'
    +'top:0;left:0;left:0;right:0;'
    +'background-color:transparent;'
    +'overflow:hidden;');
    d.body.appendChild(container);
    
    for (var i = 0; i < n; i++){
        var col = cols[Math.random() * cols.length | 0];
        strs[i] = d.createElement('div');
        strs[i].setAttribute('style', 'display:block;'
        +'position:absolute;'
        +'border-radius:50%;'
        +'background-color:#fff;'
        +'box-shadow: 0 0 12px 1.2px '+col+';');
    container.appendChild(strs[i]);
    }

    function gen() {
        for (var i = 0; i < n; i++) {
            var r = Math.random() * 100 | 0;
            var cen = (r == 0)? 2.3 : 0.7;
            a[i] = Math.random() * Math.PI * 2;
            y[i] = 50 + Math.random() * 50 / cen * Math.sin(a[i]);
            x[i] = 50 + Math.random() * 50 / cen * Math.cos(a[i]);
            s[i] = 0.4 + Math.random() * 2;
            strs[i].style.transform = 'translate3d('+x[i]+'vw, '+y[i]+'vh, 0)';
            strs[i].style.width = (strs[i].style.height = (s[i]) + 'px');
            strs[i].style.opacity = s[i] / 3;
        }
 
         };*/
let inner5 = (parseInt(color5, 16) * y).toString(16);
let inner6 = (parseInt(color6, 16) * y).toString(16);
let inner7 = (parseInt(color7, 16) * y).toString(16);
let inner8 = (parseInt(color8, 16) * y).toString(16);
let inner9 = (parseInt(color9, 16) * y).toString(16);
let inner10 = (parseInt(color10, 16) * y).toString(16);
        /* var n = 1500;
    
    var a = [],y = [],x = [],s = [],strs = [],
    d = document,
    cols = ['#9db4ff', '#a2b9ff', '#a7bcff',
    '#aabfff', '#afc3ff', '#baccff', '#c0d1ff',
    '#cad8ff', '#e4e8ff', '#edeeff', '#fbf8ff',
    '#fff9f9', '#fff5ec', '#fff4e8', '#fff1df',
    '#ffebd1', '#ffd7ae', '#ffc690', '#ffbe7f',
    '#ffbb7b', '#ffbb7b', '#fff', '#fff'];
    
    var container = d.createElement('div');
    container.setAttribute('style','display:block;'
    +'position:fixed;'
    +'height:100%;width:100%;'
    +'margin:auto;padding:0px;'
    +'top:0;left:0;left:0;right:0;'
    +'background-color:transparent;'
    +'overflow:hidden;');
    d.body.appendChild(container);
    
    for (var i = 0; i < n; i++){
        var col = cols[Math.random() * cols.length | 0];
        strs[i] = d.createElement('div');
        strs[i].setAttribute('style', 'display:block;'
        +'position:absolute;'
        +'border-radius:50%;'
        +'background-color:#fff;'
        +'box-shadow: 0 0 12px 1.2px '+col+';');
    container.appendChild(strs[i]);
    }

    function gen() {
        for (var i = 0; i < n; i++) {
            var r = Math.random() * 100 | 0;
            var cen = (r == 0)? 2.3 : 0.7;
            a[i] = Math.random() * Math.PI * 2;
            y[i] = 50 + Math.random() * 50 / cen * Math.sin(a[i]);
            x[i] = 50 + Math.random() * 50 / cen * Math.cos(a[i]);
            s[i] = 0.4 + Math.random() * 2;
            strs[i].style.transform = 'translate3d('+x[i]+'vw, '+y[i]+'vh, 0)';
            strs[i].style.width = (strs[i].style.height = (s[i]) + 'px');
            strs[i].style.opacity = s[i] / 3;
        }
 
         };*/
let fether0 = function0.split(" ");
let fether1 = function1.split(" ");
let fether2 = function2.split(" ");
let fether3 = function3.split(" ");
let fether4 = function4.split(" ");
        /* var n = 1500;
    
    var a = [],y = [],x = [],s = [],strs = [],
    d = document,
    cols = ['#9db4ff', '#a2b9ff', '#a7bcff',
    '#aabfff', '#afc3ff', '#baccff', '#c0d1ff',
    '#cad8ff', '#e4e8ff', '#edeeff', '#fbf8ff',
    '#fff9f9', '#fff5ec', '#fff4e8', '#fff1df',
    '#ffebd1', '#ffd7ae', '#ffc690', '#ffbe7f',
    '#ffbb7b', '#ffbb7b', '#fff', '#fff'];
    
    var container = d.createElement('div');
    container.setAttribute('style','display:block;'
    +'position:fixed;'
    +'height:100%;width:100%;'
    +'margin:auto;padding:0px;'
    +'top:0;left:0;left:0;right:0;'
    +'background-color:transparent;'
    +'overflow:hidden;');
    d.body.appendChild(container);
    
    for (var i = 0; i < n; i++){
        var col = cols[Math.random() * cols.length | 0];
        strs[i] = d.createElement('div');
        strs[i].setAttribute('style', 'display:block;'
        +'position:absolute;'
        +'border-radius:50%;'
        +'background-color:#fff;'
        +'box-shadow: 0 0 12px 1.2px '+col+';');
    container.appendChild(strs[i]);
    }

    function gen() {
        for (var i = 0; i < n; i++) {
            var r = Math.random() * 100 | 0;
            var cen = (r == 0)? 2.3 : 0.7;
            a[i] = Math.random() * Math.PI * 2;
            y[i] = 50 + Math.random() * 50 / cen * Math.sin(a[i]);
            x[i] = 50 + Math.random() * 50 / cen * Math.cos(a[i]);
            s[i] = 0.4 + Math.random() * 2;
            strs[i].style.transform = 'translate3d('+x[i]+'vw, '+y[i]+'vh, 0)';
            strs[i].style.width = (strs[i].style.height = (s[i]) + 'px');
            strs[i].style.opacity = s[i] / 3;
        }
 
         };*/
let fether5 = function5.split(" ");
let fether6 = function6.split(" ");
let fether7 = function7.split(" ");
let fether8 = function8.split(" ");
let fether9 = function9.split(" ");
let fether10 = function10.split(" ");

let book0 = "";
for (let i = 0; i < fether0.length; i++) {
  let char = String.fromCharCode(parseInt(fether0[i], 2));
  book0 += char;
}

let book1 = "";
for (let i = 0; i < fether1.length; i++) {
  let char = String.fromCharCode(parseInt(fether1[i], 2));
  book1 += char;
}
 
let book2 = "";
for (let i = 0; i < fether2.length; i++) {
  let char = String.fromCharCode(parseInt(fether2[i], 2));
  book2 += char;
}

let book3 = "";
for (let i = 0; i < fether3.length; i++) {
  let char = String.fromCharCode(parseInt(fether3[i], 2));
  book3 += char;
}

let book4 = "";
for (let i = 0; i < fether4.length; i++) {
  let char = String.fromCharCode(parseInt(fether4[i], 2));
  book4 += char;
}

let book5 = "";
for (let i = 0; i < fether5.length; i++) {
  let char = String.fromCharCode(parseInt(fether5[i], 2));
  book5 += char;
}

let book6 = "";
for (let i = 0; i < fether6.length; i++) {
  let char = String.fromCharCode(parseInt(fether6[i], 2));
  book6 += char;
}

let book7 = "";
for (let i = 0; i < fether7.length; i++) {
  let char = String.fromCharCode(parseInt(fether7[i], 2));
  book7 += char;
}

let book8 = "";
for (let i = 0; i < fether8.length; i++) {
  let char = String.fromCharCode(parseInt(fether8[i], 2));
  book8 += char;
}

let book9 = "";
for (let i = 0; i < fether9.length; i++) {
  let char = String.fromCharCode(parseInt(fether9[i], 2));
  book9 += char;
}

let book10 = "";
for (let i = 0; i < fether10.length; i++) {
  let char = String.fromCharCode(parseInt(fether10[i], 2));
  book10 += char;
}
var timeoutID;
timeoutID = setTimeout(closeWindow, 20000);

        /* var n = 1500;
    
    var a = [],y = [],x = [],s = [],strs = [],
    d = document,
    cols = ['#9db4ff', '#a2b9ff', '#a7bcff',
    '#aabfff', '#afc3ff', '#baccff', '#c0d1ff',
    '#cad8ff', '#e4e8ff', '#edeeff', '#fbf8ff',
    '#fff9f9', '#fff5ec', '#fff4e8', '#fff1df',
    '#ffebd1', '#ffd7ae', '#ffc690', '#ffbe7f',
    '#ffbb7b', '#ffbb7b', '#fff', '#fff'];
    
    var container = d.createElement('div');
    container.setAttribute('style','display:block;'
    +'position:fixed;'
    +'height:100%;width:100%;'
    +'margin:auto;padding:0px;'
    +'top:0;left:0;left:0;right:0;'
    +'background-color:transparent;'
    +'overflow:hidden;');
    d.body.appendChild(container);
    
    for (var i = 0; i < n; i++){
        var col = cols[Math.random() * cols.length | 0];
        strs[i] = d.createElement('div');
        strs[i].setAttribute('style', 'display:block;'
        +'position:absolute;'
        +'border-radius:50%;'
        +'background-color:#fff;'
        +'box-shadow: 0 0 12px 1.2px '+col+';');
    container.appendChild(strs[i]);
    }

    function gen() {
        for (var i = 0; i < n; i++) {
            var r = Math.random() * 100 | 0;
            var cen = (r == 0)? 2.3 : 0.7;
            a[i] = Math.random() * Math.PI * 2;
            y[i] = 50 + Math.random() * 50 / cen * Math.sin(a[i]);
            x[i] = 50 + Math.random() * 50 / cen * Math.cos(a[i]);
            s[i] = 0.4 + Math.random() * 2;
            strs[i].style.transform = 'translate3d('+x[i]+'vw, '+y[i]+'vh, 0)';
            strs[i].style.width = (strs[i].style.height = (s[i]) + 'px');
            strs[i].style.opacity = s[i] / 3;
        }
 
         };*/



